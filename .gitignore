# Environment files — NEVER commit credentials
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# SQLite data + WAL/SHM
server/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Uploads (user-generated content — not source)
server/public/uploads/

# Logs
logs/
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Claude Code local harness config
.claude/

# Build artifacts
# (dist/ is published to the gh-pages branch via scripts/deploy-pages.sh,
#  not tracked on main)
dist/
build/

# Local backups
backups/

# Demo export bundle (generated artifact, not source)
demo.zip
